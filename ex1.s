LED_PORT_BASE EQU 0x0001_0000


START
    LI t0, 0x11
    SW t0, LED_PORT_BASE, t1

    CALL WAITSECOND

    LI t0, 0x31
    SW t0, LED_PORT_BASE, t1

    CALL WAITSECOND

    LI t0, 0x41
    SW t0, LED_PORT_BASE, t1

    CALL WAITSECOND
    CALL WAITSECOND
    CALL WAITSECOND

    LI t0, 0x21
    SW t0, LED_PORT_BASE, t1

    CALL WAITSECOND

    LI t0, 0x11
    SW t0, LED_PORT_BASE, t1

    CALL WAITSECOND

    LI t0, 0x13
    SW t0, LED_PORT_BASE, t1

    CALL WAITSECOND

    LI t0, 0x14
    SW t0, LED_PORT_BASE, t1

    CALL WAITSECOND
    CALL WAITSECOND
    CALL WAITSECOND

    LI t0, 0x12
    SW t0, LED_PORT_BASE, t1

    CALL WAITSECOND

JAL START

WAITSECOND

    LI a2, 0x989690

LOOP_SUBS
    SUBI a2, a2, 0x1 
    BNE a2, zero, LOOP_SUBS
    RET

